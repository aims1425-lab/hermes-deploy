# Roadmap

## Near term

- Add optional automated integration test for `docker-compose.yml` in environments with Docker available.
- Publish a short production hardening checklist in `docs/SECURITY.md`.
- Add a minimal shell-based smoke test for the installer output.

## Medium term

- Add checks for common configuration mistakes in `.env.example` and `config.yaml`.
- Add a release workflow for tagged versions.

## Long term

- Expand examples for alternate deployment targets and provider-specific defaults.
